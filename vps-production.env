# VPS Production Environment Configuration for yarukai.com
# Location: /var/www/html/pazar/server/.env

# === DEPLOYMENT SETTINGS ===
NODE_ENV=production
CLIENT_URL=https://yarukai.com
PORT=3000

# === PDF URL GENERATION ===
# SERVER_BASE_URL should be commented out for same-domain deployment
# This forces the app to use relative URLs like /shipping/file.pdf
# which resolves to https://yarukai.com/shipping/file.pdf
#SERVER_BASE_URL=https://yarukai.com

# === DATABASE CONFIGURATION ===
# Update these with your actual database credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pazar_plus
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# === SECURITY ===
SESSION_SECRET=your-super-secret-key-change-this-in-production

# === CORS SETTINGS ===
CORS_ORIGIN=https://yarukai.com

# === LOGGING ===
LOG_LEVEL=info

# === OPTIONAL SETTINGS ===
# Uncomment if you need to disable background jobs for faster startup
#DISABLE_BACKGROUND_JOBS=true

# === SSL/TLS (if using custom certificates) ===
#SSL_CERT_PATH=/path/to/certificate.crt
#SSL_KEY_PATH=/path/to/private.key
